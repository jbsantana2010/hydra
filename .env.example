DATABASE_URL=postgresql+psycopg://hydra:hydra@postgres:5432/hydra
REDIS_URL=redis://redis:6379/0
APP_ENV=local
POSTGRES_DB=hydra
POSTGRES_USER=hydra
POSTGRES_PASSWORD=hydra
HYDRA_BASIC_USER=admin
HYDRA_BASIC_PASS=change-me
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
HYDRA_LLM_PRIMARY=anthropic
HYDRA_LLM_FALLBACK=openai
HYDRA_LLM_PER_CALL_CAP_USD=0.50
# Kit generation uses guarded per-document calls capped at $0.18 each.
# Keep this global cap at or above 0.18 when generating professional kits.
